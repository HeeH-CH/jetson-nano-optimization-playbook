# Deployment

This theme tracks:

- inference runtimes
- TensorRT / ONNX deployment paths
- Jetson containers
- camera pipeline choices
- model/runtime packaging decisions

Current subtopics:

- `onnx-tensorrt-guide/`
